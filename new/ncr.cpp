#include <bits/stdc++.h>
#include <math.h>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    long long int t,n,r,i,j,m;
    cin>>t;
    while(t--)
    {
        cin>>n>>r;
        m=n-r;
        if (m>r)
        {
            for (i=n;i>m;i--)
            {

            }
        }
        else
        {

        }
    }
}
