#include <stdio.h>

int main()
{
    int i,j,n;
    long long int arr[i];
    int b;

    scanf("%d",&i);

    for (j=0;j<=n;j++)
    {
        scanf("%d" , &arr[j]);
        if (arr[0]==0)
        {
            b++;
            printf("%d",b);
        }
    }
}
